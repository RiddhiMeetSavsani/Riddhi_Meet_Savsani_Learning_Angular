import { AvailabilityStylePipe } from './availability-style.pipe';

describe('AvailabilityStylePipe', () => {
  it('create an instance', () => {
    const pipe = new AvailabilityStylePipe();
    expect(pipe).toBeTruthy();
  });
});
